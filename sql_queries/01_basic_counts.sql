-- This query counts the number of entries in compount_structures.
-- Question to answer: How many entries are found in compound_structures? Can we give it an easier name?
SELECT COUNT(*) AS total_compounds
FROM compound_structures;
